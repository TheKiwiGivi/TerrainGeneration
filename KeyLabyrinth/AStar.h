#pragma once
//#include "Tile.h"
//#include "Level.h"
#include "glm/glm.hpp"
#include "map.h"
#include <iostream>
#include <set>
#include <vector>
#include <map>

/*
class Node;

class AStar {
private:
	std::vector<Node*> nodes;
public:
	AStar(Map *map);
	std::vector<glm::ivec2> getPath(glm::ivec2 startPosition, glm::ivec2 goalPosition);
	void addNode(Node *node);
	Node *getNodeAtPosition(glm::ivec2 position);
};
*/